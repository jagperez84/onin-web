export type AddressLookupResult={display_name:string;lat:string;lon:string;address?:{road?:string;house_number?:string;postcode?:string;city?:string;town?:string;village?:string;municipality?:string;state?:string;country_code?:string;country?:string}};
export async function searchAddress(query:string):Promise<AddressLookupResult[]>{const term=query.trim();if(term.length<4)return[];const url=new URL('https://nominatim.openstreetmap.org/search');url.searchParams.set('q',term);url.searchParams.set('format','jsonv2');url.searchParams.set('addressdetails','1');url.searchParams.set('limit','5');const response=await fetch(url.toString(),{headers:{Accept:'application/json'}});if(!response.ok)throw new Error(`No se pudo consultar OpenStreetMap (${response.status}).`);return await response.json() as AddressLookupResult[];}
