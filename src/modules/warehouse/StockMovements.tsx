import { FormEvent, useEffect, useState } from "react";
import { ArrowDownToLine, ArrowUpFromLine, Save, Ruler } from "lucide-react";
import { useLocation, useNavigate } from "react-router-dom";
import { getActiveCompanies } from "../../services/core/coreRepository";
import { listWarehouses, type Warehouse } from "../../services/warehouse/warehouseRepository";
import { listMovementTypes, listStockCharacteristics, listStockMovements, registerStockMovement, type StockMovement, type StockProduct, type StockCharacteristic } from "../../services/warehouse/stockRepository";
import { getProductLineDefinition, type ProductDimensionDefinition } from "../../services/catalog/productDefinitionRepository";
import { StockProductLookup } from "./StockProductLookup";
import "./stock.css";

type ProductDimension = ProductDimensionDefinition & { id: string };

async function listProductDimensions(productId: number): Promise<ProductDimension[]> {
  const definition = await getProductLineDefinition(productId);
  return definition.dimensions.map((d) => ({ ...d, id: `${productId}-${d.dimension_number}` }));
}

export function StockMovements(){const location=useLocation();const navigate=useNavigate();const[successMessage,setSuccessMessage]=useState("");useEffect(()=>{const message=(location.state as{stockSuccess?:string}|null)?.stockSuccess;if(!message)return;setSuccessMessage(message);navigate(location.pathname,{replace:true,state:null});},[location.pathname,location.state,navigate]);return <div className="stock-page"><StockMovementList successMessage={successMessage}/><StockMovementCreate/></div>}

function StockMovementList({successMessage}:{successMessage?:string}){const[companyId,setCompanyId]=useState<number|null>(null);const[warehouses,setWarehouses]=useState<Warehouse[]>([]);const[rows,setRows]=useState<StockMovement[]>([]);const[warehouseId,setWarehouseId]=useState<number|undefined>();const[from,setFrom]=useState("");const[to,setTo]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{getActiveCompanies().then(c=>setCompanyId(c[0]?.id??null)).catch(e=>setError(e instanceof Error?e.message:"No se pudo cargar la empresa."));},[]);useEffect(()=>{if(companyId===null)return;Promise.all([listWarehouses(companyId,"","active"),listStockMovements(companyId,{warehouseId,from,to})]).then(([w,m])=>{setWarehouses(w);setRows(m)}).catch(e=>setError(e instanceof Error?e.message:"No se pudieron cargar los movimientos.")).finally(()=>setLoading(false))},[companyId,warehouseId]);async function load(){if(companyId===null)return;setLoading(true);setError("");try{setRows(await listStockMovements(companyId,{warehouseId,from,to}))}catch(e){setError(e instanceof Error?e.message:"No se pudieron cargar los movimientos.")}finally{setLoading(false)}}return <section><div className="page-head"><div><div className="eyebrow">ALMACÉN / MOVIMIENTOS</div><h1>Movimientos de stock</h1><p>Entradas, salidas, ajustes y movimientos generados por traspasos.</p></div><a className="stock-button primary" href="/almacen/movimientos/nuevo">Nuevo movimiento</a></div>{successMessage&&<div className="inline-success">{successMessage}</div>}<div className="stock-toolbar"><select value={warehouseId??""} onChange={e=>setWarehouseId(e.target.value?Number(e.target.value):undefined)}><option value="">Todos los almacenes</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.code} · {w.name}</option>)}</select><label className="compact-field"><span>Desde</span><input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label className="compact-field"><span>Hasta</span><input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><button className="stock-button primary" onClick={()=>void load()}>Filtrar</button></div>{error&&<div className="inline-error">{error}</div>}<div className="stock-panel"><div className="stock-table-wrap"><table className="stock-table"><thead><tr><th>Fecha</th><th>Tipo</th><th>Almacén</th><th>Artículo</th><th>Característica</th><th>Dimensiones</th><th className="numeric">Cantidad</th><th>Referencia</th></tr></thead><tbody>{loading?<tr><td colSpan={8}>Cargando…</td></tr>:rows.length===0?<tr><td colSpan={8} className="empty">No hay movimientos.</td></tr>:rows.map(r=><tr key={r.id}><td>{new Date(r.movement_date).toLocaleString("es-ES")}</td><td><span className={`movement-badge ${r.movement_type?.direction===1?"in":"out"}`}>{r.movement_type?.direction===1?<ArrowDownToLine size={14}/>:<ArrowUpFromLine size={14}/>} {r.movement_type?.name}</span></td><td>{r.warehouse?.code}</td><td><strong>{r.product?.code}</strong><span className="secondary-line">{r.product?.commercial_description||"Sin descripción"}</span></td><td>{r.characteristic?.code||"—"}</td><td>{formatDimensions(r.dimension_values)}</td><td className="numeric">{r.quantity}</td><td>{r.reference||"—"}</td></tr>)}</tbody></table></div></div></section>}

function formatDimensions(value:Record<string,unknown>|null|undefined){if(!value||typeof value!=="object")return "—";return Object.entries(value).map(([key,val])=>`${key}: ${val}`).join(" · ")||"—"}

function StockMovementCreate(){const navigate=useNavigate();const[companyId,setCompanyId]=useState<number|null>(null);const[warehouses,setWarehouses]=useState<Warehouse[]>([]);const[types,setTypes]=useState<{id:number;code:string;name:string;direction:number}[]>([]);const[product,setProduct]=useState<StockProduct|null>(null);const[characteristics,setCharacteristics]=useState<StockCharacteristic[]>([]);const[dimensions,setDimensions]=useState<ProductDimension[]>([]);const[dimensionValues,setDimensionValues]=useState<Record<string,string>>({});const[warehouseId,setWarehouseId]=useState("");const[type,setType]=useState("");const[characteristicId,setCharacteristicId]=useState("");const[quantity,setQuantity]=useState("");const[reference,setReference]=useState("");const[notes,setNotes]=useState("");const[date,setDate]=useState(new Date().toISOString().slice(0,16));const[error,setError]=useState("");const[saving,setSaving]=useState(false);
useEffect(()=>{getActiveCompanies().then(c=>{const id=c[0]?.id??null;setCompanyId(id);if(id)Promise.all([listWarehouses(id,"","active"),listMovementTypes(id)]).then(([w,t])=>{setWarehouses(w);setTypes(t.filter(x=>!["TRANSFER_IN","TRANSFER_OUT"].includes(x.code)));setType(t.find(x=>x.code==="ADJUSTMENT_IN")?.code??"")}).catch(e=>setError(e instanceof Error?e.message:"No se pudieron cargar los datos."))}).catch(e=>setError(e instanceof Error?e.message:"No se pudo cargar la empresa."))},[]);
useEffect(()=>{if(!product){setCharacteristics([]);setDimensions([]);setDimensionValues({});return;}Promise.all([listStockCharacteristics(product.id),listProductDimensions(product.id)]).then(([c,d])=>{setCharacteristics(c);setDimensions(d);setDimensionValues(Object.fromEntries(d.map(x=>[x.code,""])))}).catch(e=>{setCharacteristics([]);setDimensions([]);setDimensionValues({});setError(e instanceof Error?e.message:"No se pudieron cargar las dimensiones del artículo.")});setCharacteristicId("")},[product?.id]);
async function submit(e:FormEvent){e.preventDefault();if(companyId===null||!product)return;setSaving(true);setError("");try{const normalizedDimensions=Object.fromEntries(dimensions.map(d=>[d.code,dimensionValues[d.code]===undefined||dimensionValues[d.code]===""?null:Number(dimensionValues[d.code]) ]));if(dimensions.some(d=>normalizedDimensions[d.code]===null))throw new Error("Completa todas las dimensiones del artículo.");await registerStockMovement({companyId,warehouseId:Number(warehouseId),productId:product.id,quantity:Number(quantity),movementTypeCode:type,characteristicId:characteristicId?Number(characteristicId):null,dimensionValues:normalizedDimensions,reference,notes,movementDate:date});navigate("/almacen/movimientos",{replace:true,state:{stockSuccess:"Movimiento registrado correctamente."}})}catch(e){setError(e instanceof Error?e.message:"No se pudo registrar el movimiento.")}finally{setSaving(false)}}
return <section className="stock-form-panel"><div className="panel-title"><div><h2>Nuevo movimiento</h2><p>Los movimientos actualizan el saldo de existencias de forma transaccional.</p></div></div>{error&&<div className="inline-error">{error}</div>}<form className="stock-form-grid" onSubmit={submit}><StockProductLookup companyId={companyId??0} value={product} onChange={setProduct}/><label><span>Almacén *</span><select value={warehouseId} onChange={e=>setWarehouseId(e.target.value)} required><option value="">Seleccionar…</option>{warehouses.map(w=><option key={w.id} value={w.id}>{w.code} · {w.name}</option>)}</select></label><label><span>Tipo *</span><select value={type} onChange={e=>setType(e.target.value)} required>{types.map(t=><option key={t.code} value={t.code}>{t.name}</option>)}</select></label><label><span>Característica / color</span><select value={characteristicId} onChange={e=>setCharacteristicId(e.target.value)} disabled={!product}><option value="">Sin característica</option>{characteristics.map(c=><option key={c.id} value={c.id}>{c.code}{c.description?` · ${c.description}`:""}</option>)}</select></label>{dimensions.length>0&&<div className="stock-dimension-fields field-wide"><div className="stock-dimension-title"><Ruler size={15}/> Dimensiones del artículo</div><div className="stock-dimension-grid">{dimensions.map(d=><label key={d.id}><span>{d.name} <small>({d.code})</small> *</span><div className="stock-dimension-input"><input type="number" min="0" step={d.decimals?`0.${"0".repeat(Math.max(0,d.decimals-1))}1`:"1"} value={dimensionValues[d.code]??""} onChange={e=>setDimensionValues(v=>({...v,[d.code]:e.target.value}))}/><em>u.{d.unit_id}</em></div></label>)}</div></div>}<label><span>Cantidad *</span><input inputMode="decimal" type="number" min="0.0001" step="any" value={quantity} onChange={e=>setQuantity(e.target.value)} required/></label><label><span>Fecha y hora</span><input type="datetime-local" value={date} onChange={e=>setDate(e.target.value)}/></label><label><span>Referencia</span><input value={reference} onChange={e=>setReference(e.target.value)} maxLength={255}/></label><label className="field-wide"><span>Observaciones</span><textarea value={notes} onChange={e=>setNotes(e.target.value)}/></label><div className="stock-form-actions field-wide"><button className="stock-button primary" disabled={saving}><Save size={15}/>{saving?"Guardando…":"Registrar movimiento"}</button></div></form></section>}
