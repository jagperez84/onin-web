import { useEffect, useMemo, useRef, useState } from 'react';
import { createPortal } from 'react-dom';
import { ArrowLeft, ChevronDown, ChevronUp, Layers, Pencil, Plus, Search, Trash2, X, Eye, SlidersHorizontal, Sparkles } from 'lucide-react';
import { Link, useNavigate } from 'react-router-dom';
import { CoreRepositoryError } from '../../services/core/coreRepository';
import { getProductLineDefinition, type ProductLineDefinition } from '../../services/catalog/productDefinitionRepository';
import { createQuotation, customerAddresses, customerProductDiscount, quotationOptions, type ProductLineBehavior, type QuotationLineCharacteristicDraft, type QuotationLineDimensionDraft } from '../../services/sales/quotationCreationRepository';
import { calculateQuotationLineByProductId, type QuotationLineSnapshot } from '../../services/sales/quotationLineCalculationService';
import { QuotationLineConfigurator } from './QuotationLineConfigurator';
import { QuotationLineSnapshotModal } from './QuotationLineSnapshotModal';
import { MessageLog } from '../../components/ui/MessageLog';
import { ProfileSaveBar } from '../../components/ui/ProfileSaveBar';
import { Toast } from '../../components/ui/Toast';
import './quotation-create.css';
import './quotation-configurator.css';

const today = () => new Date().toISOString().slice(0, 10);
const money = (n: number) => n.toLocaleString('es-ES', { style: 'currency', currency: 'EUR' });

type AddressDraft = { source_id: number | null; label: string; street: string; postal_code: string; city: string; region: string };
type Line = {
  product_id: number | null;
  description: string;
  quantity: number;
  unit_price: number;
  discount_percent: number;
  line_behavior: ProductLineBehavior | null;
  product_definition_snapshot: ProductLineDefinition | null;
  dimensions: QuotationLineDimensionDraft[];
  characteristics: QuotationLineCharacteristicDraft[];
  specific_data: Record<string, unknown>;
  configuration_snapshot?: QuotationLineSnapshot | null;
};

const emptyAddress = (): AddressDraft => ({ source_id: null, label: '', street: '', postal_code: '', city: '', region: '' });
const blank = (): Line => ({ product_id: null, description: '', quantity: 1, unit_price: 0, discount_percent: 0, line_behavior: null, product_definition_snapshot: null, dimensions: [], characteristics: [], specific_data: {}, configuration_snapshot: null });
type Option = { id: number; label: string; code?: string; price?: number; lineBehavior?: ProductLineBehavior | null };

function clone<T>(value: T): T { return JSON.parse(JSON.stringify(value)); }
function dimensionsFromDefinition(definition: ProductLineDefinition) { return definition.dimensions.map((d, i) => ({ code: d.code, name: d.name, value: null, unit_id: d.unit_id, sort_order: i })); }
function characteristicsFromDefinition(definition: ProductLineDefinition) { return definition.characteristics.map(c => ({ attribute_id: c.attribute_id, attribute_value_id: null, value_text: null, value_number: null, value_boolean: null })); }
function hasRequiredCharacteristicValues(line: Line) { return (line.product_definition_snapshot?.characteristics ?? []).every((c, i) => !c.required || [line.characteristics[i]?.attribute_value_id, line.characteristics[i]?.value_text, line.characteristics[i]?.value_number, line.characteristics[i]?.value_boolean].some(v => v !== null && v !== undefined && v !== '')); }

export function QuotationCreate() {
  const nav = useNavigate();
  const [opts, setOpts] = useState<any>();
  const [addresses, setAddresses] = useState<any[]>([]);
  const [customerId, setCustomerId] = useState<number | null>(null);
  const [commercialId, setCommercialId] = useState<number | null>(null);
  const [warehouseId, setWarehouseId] = useState<number | null>(null);
  const [billingId, setBillingId] = useState<number | null>(null);
  const [installationId, setInstallationId] = useState<number | null>(null);
  const [billingAddress, setBillingAddress] = useState<AddressDraft>(emptyAddress());
  const [installationAddress, setInstallationAddress] = useState<AddressDraft>(emptyAddress());
  const [paymentMethodId, setPaymentMethodId] = useState<number | null>(null);
  const [paymentTermId, setPaymentTermId] = useState<number | null>(null);
  const [taxRateId, setTaxRateId] = useState<number | null>(null);
  const [taxPercent, setTaxPercent] = useState(21);
  const [issueDate, setIssueDate] = useState(today());
  const [validUntil, setValidUntil] = useState('');
  const [reference, setReference] = useState('');
  const [notes, setNotes] = useState('');
  const [lines, setLines] = useState<Line[]>([blank()]);
  const [error, setError] = useState('');
  const [toast, setToast] = useState('');
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [headerCollapsed, setHeaderCollapsed] = useState(false);
  const [expandedLines, setExpandedLines] = useState<Set<number>>(new Set());
  const [pendingCustomerId, setPendingCustomerId] = useState<number | null>(null);
  const [changingCustomer, setChangingCustomer] = useState(false);
  const [configuratorOpen, setConfiguratorOpen] = useState(false);
  const [configuratorLineIndex, setConfiguratorLineIndex] = useState<number | null>(null);
  const [snapshotModalOpen, setSnapshotModalOpen] = useState(false);
  const [selectedSnapshot, setSelectedSnapshot] = useState<QuotationLineSnapshot | null>(null);
  const [selectedSnapshotLineNo, setSelectedSnapshotLineNo] = useState<number>(1);
  const calculationRequests = useRef<Record<number, number>>({});

  useEffect(() => { void quotationOptions().then(setOpts).catch(e => setError(e instanceof Error ? e.message : 'No se pudieron cargar los datos.')).finally(() => setLoading(false)); }, []);
  const totals = useMemo(() => lines.reduce((a, l) => { const gross=Math.max(0,l.quantity*l.unit_price); const discount=Math.max(0,gross*l.discount_percent/100); const net=Math.max(0,gross-discount); const tax=net*taxPercent/100; return {discount:a.discount+discount,net:a.net+net,tax:a.tax+tax,total:a.total+net+tax}; }, {discount:0,net:0,tax:0,total:0}), [lines,taxPercent]);
  const updateLine = (i:number,patch:Partial<Line>) => setLines(xs=>xs.map((l,j)=>j===i?{...l,...patch}:l));

  const recalculateLine = async (i:number,productId:number|null,dimensions:QuotationLineDimensionDraft[],characteristics:QuotationLineCharacteristicDraft[]=[],silent=true) => {
    if(productId===null)return;
    const request=(calculationRequests.current[i]??0)+1; calculationRequests.current[i]=request;
    try{const result=await calculateQuotationLineByProductId({productId,dimensions,characteristics});if(calculationRequests.current[i]===request)updateLine(i,{unit_price:result.unit_price,specific_data:{...((lines[i]?.specific_data??{}) as Record<string,unknown>),price_missing:result.price_missing,price_missing_reason:result.price_missing_reason??null}});}
    catch(e){if(!silent&&calculationRequests.current[i]===request)setError(e instanceof Error?e.message:'No se pudo recalcular el precio del artículo.');}
  };

  const selectProduct = async (i:number,id:number|null) => {
    if(id===null){calculationRequests.current[i]=(calculationRequests.current[i]??0)+1;updateLine(i,blank());setExpandedLines(s=>{const n=new Set(s);n.delete(i);return n;});return;}
    const p=opts?.products?.find((x:any)=>x.id===id);setError('');
    try{const definition=await getProductLineDefinition(id);const discount=customerId?await customerProductDiscount(customerId,id):null;const line:Line={product_id:id,description:p?.label||p?.code||'',quantity:1,unit_price:Number(p?.price??0),discount_percent:discount?.discount_percent??0,line_behavior:p?.lineBehavior??null,product_definition_snapshot:clone(definition),dimensions:dimensionsFromDefinition(definition),characteristics:characteristicsFromDefinition(definition),specific_data:{price_missing:false}};updateLine(i,line);setExpandedLines(s=>new Set(s).add(i));void recalculateLine(i,id,line.dimensions,line.characteristics,true);
      const hasConfig=Boolean((definition.dimensions&&definition.dimensions.length>0)||(definition.characteristics&&definition.characteristics.length>0)||definition.measurement_type_id!=null||(p?.lineBehavior&&(p.lineBehavior.is_configurable||p.lineBehavior.confectionable||p.lineBehavior.recuttable)));
      if(hasConfig){setConfiguratorLineIndex(i);setConfiguratorOpen(true);setToast(`El artículo "${p?.label||p?.code}" tiene configuración aplicable. Abriendo configurador guiado...`);}else if(discount)setToast(`El cliente tiene descuentos aplicados (${discount.level==='article'?'artículo':'familia'}: ${discount.discount_percent}%).`);
    }catch(e){setError(e instanceof Error?e.message:'No se pudo cargar la definición del artículo.');}
  };
  const openLineConfigurator=(lineIndex:number)=>{setConfiguratorLineIndex(lineIndex);setConfiguratorOpen(true);};
  const handleConfiguratorConfirm=(snapshot:QuotationLineSnapshot)=>{if(configuratorLineIndex===null)return;const p=opts?.products?.find((x:any)=>x.id===snapshot.article.id);const updatedLine:Line={product_id:snapshot.article.id,description:snapshot.article.commercial_description||snapshot.article.technical_description||snapshot.article.code,quantity:snapshot.quantity,unit_price:snapshot.pricing.unit_price,discount_percent:snapshot.pricing.discount_percent,line_behavior:p?.lineBehavior??null,product_definition_snapshot:clone(lines[configuratorLineIndex]?.product_definition_snapshot),dimensions:snapshot.dimensions.map((d,di)=>({code:d.code,name:d.name,value:d.value,unit_id:d.unit_id,sort_order:di})),characteristics:snapshot.selected_attributes.map(a=>({attribute_id:a.attribute_id,attribute_value_id:a.value_id,value_text:a.value_text??null,value_number:a.value_number??null,value_boolean:a.value_boolean??null})),specific_data:{configuration_snapshot:snapshot,price_missing:snapshot.pricing.price_missing,price_missing_reason:snapshot.pricing.price_missing_reason??null},configuration_snapshot:snapshot};updateLine(configuratorLineIndex,updatedLine);setToast(snapshot.pricing.price_missing?`Línea ${configuratorLineIndex+1}: no hay precio para esta combinación. Añádelo manualmente.`:`Línea ${configuratorLineIndex+1} configurada correctamente con snapshot.`);};
  const openSnapshotModal=(snapshot:QuotationLineSnapshot,lineNo:number)=>{setSelectedSnapshot(snapshot);setSelectedSnapshotLineNo(lineNo);setSnapshotModalOpen(true);};
  const updateDimensionValue=(i:number,d:number,value:number|null)=>{const line=lines[i];if(!line)return;const dimensions=line.dimensions.map((x,k)=>k===d?{...x,value}:x);updateLine(i,{dimensions});if(line.product_id!==null)void recalculateLine(i,line.product_id,dimensions,line.characteristics,false);};
  const updateCharacteristic=(i:number,c:number,patch:Partial<QuotationLineCharacteristicDraft>)=>{const line=lines[i];if(!line)return;const characteristics=line.characteristics.map((x,k)=>k===c?{...x,...patch}:x);updateLine(i,{characteristics});if(line.product_id!==null)void recalculateLine(i,line.product_id,line.dimensions,characteristics,false);};
  const toggleLine=(i:number)=>setExpandedLines(s=>{const next=new Set(s);if(next.has(i))next.delete(i);else next.add(i);return next;});

  const applyCustomerAddresses=(rows:any[])=>{setAddresses(rows);const billing=rows.find((a:any)=>['FACTURACION','FISCAL'].includes(String(a.address_type).toUpperCase()));const installation=rows.find((a:any)=>String(a.address_type).toUpperCase()==='INSTALACION');setBillingId(billing?.id??null);setInstallationId(installation?.id??null);setBillingAddress(addressDraft(billing));setInstallationAddress(addressDraft(installation));};
  useEffect(()=>{if(!customerId){setAddresses([]);setBillingId(null);setInstallationId(null);setBillingAddress(emptyAddress());setInstallationAddress(emptyAddress());return;}void customerAddresses(customerId).then(applyCustomerAddresses).catch(e=>setError(e instanceof Error?e.message:'No se pudieron cargar las direcciones del cliente.'));},[customerId]);
  const requestCustomerChange=(id:number|null)=>{if(id===customerId)return;setPendingCustomerId(id);}; const cancelCustomerChange=()=>setPendingCustomerId(null);
  const confirmCustomerChange=async()=>{const id=pendingCustomerId;setPendingCustomerId(null);if(id===customerId)return;setChangingCustomer(true);setError('');try{setCustomerId(id);if(id===null){setLines(xs=>xs.map(l=>({...l,discount_percent:0})));return;}const rows=await customerAddresses(id);applyCustomerAddresses(rows);const discounted=await Promise.all(lines.map(async l=>l.product_id?customerProductDiscount(id,l.product_id):null));setLines(xs=>xs.map((l,index)=>({...l,discount_percent:discounted[index]?.discount_percent??0})));if(discounted.some(Boolean))setToast('El cliente tiene descuentos aplicados.');}catch(e){setError(e instanceof Error?e.message:'No se pudieron recalcular los datos asociados al cliente.');}finally{setChangingCustomer(false);}};

  const save=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!customerId){setError('Selecciona un cliente.');return;}if(lines.some(l=>!l.description.trim())){setError('Todas las líneas deben tener descripción.');return;}if(lines.some(l=>!hasRequiredCharacteristicValues(l))){setError('Completa las características obligatorias de los artículos configurables.');return;}if(lines.some(l=>Boolean(l.specific_data?.price_missing))){setError('Hay líneas sin precio para la combinación seleccionada. Añade manualmente el precio de cada línea marcada en rojo.');return;}setSaving(true);try{await createQuotation({customer_id:customerId,commercial_id:commercialId,warehouse_id:warehouseId,billing_address_id:billingId,installation_address_id:installationId,billing_address:billingAddress,installation_address:installationAddress,payment_method_id:paymentMethodId,payment_term_id:paymentTermId,measurement_id:null,tax_rate_id:taxRateId,tax_percent:taxPercent,issue_date:issueDate,valid_until:validUntil||null,reference,notes,lines:lines.map(l=>({product_id:l.product_id,description:l.description,quantity:l.quantity,unit_price:l.unit_price,discount_percent:l.discount_percent,dimensions:l.dimensions,characteristics:l.characteristics,specific_data:{...l.specific_data,configuration_snapshot:l.configuration_snapshot||l.specific_data?.configuration_snapshot||null}}))});nav('/ventas/presupuestos');}catch(e){setError(e instanceof CoreRepositoryError?e.message:e instanceof Error?e.message:'No se pudo guardar el presupuesto.');}finally{setSaving(false);}};

  if(loading)return <div className="module-page"><div className="page-head"><div><div className="eyebrow">VENTAS / PRESUPUESTOS / NUEVO</div><h1>Nuevo presupuesto</h1></div></div><p>Cargando datos…</p></div>;
  const currentConfiguratorLine=configuratorLineIndex!==null?lines[configuratorLineIndex]:null;
  return <div className="module-page quotation-create"><div className="page-head"><div><div className="eyebrow">VENTAS / PRESUPUESTOS / NUEVO</div><h1>Nuevo presupuesto</h1><p>Crear presupuesto en estado borrador con configuración de líneas y snapshot inmutable.</p></div><Link className="secondary-button" to="/ventas/presupuestos"><ArrowLeft size={15}/>Volver a presupuestos</Link></div><MessageLog error={error}/>{toast&&<Toast message={toast} onClose={()=>setToast('')}/>}<form id="quotation-create-form" className="detail-grid" onSubmit={save}>
    <section className="panel quotation-header-panel"><div className="panel-head"><div><h2>Datos generales</h2><p>Cliente, condiciones comerciales y direcciones del presupuesto.</p></div><button type="button" className="secondary-button" onClick={()=>setHeaderCollapsed(v=>!v)}>{headerCollapsed?<><ChevronDown size={15}/>Mostrar cabecera</>:<><ChevronUp size={15}/>Contraer cabecera</>}</button></div>{!headerCollapsed&&<div className="form-grid"><label className="quotation-reference-field">Referencia<input value={reference} onChange={e=>setReference(e.target.value)}/></label><LookupSelect label="Cliente" required options={opts?.customers??[]} value={customerId} onChange={requestCustomerChange} placeholder="Buscar cliente por nombre…"/><label>Comercial<select value={commercialId??''} onChange={e=>setCommercialId(e.target.value?Number(e.target.value):null)}><option value="">Sin asignar</option>{(opts?.commercials??[]).map((x:any)=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Almacén<select value={warehouseId??''} onChange={e=>setWarehouseId(e.target.value?Number(e.target.value):null)}><option value="">Sin asignar</option>{(opts?.warehouses??[]).map((x:any)=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Fecha de documento<input type="date" value={issueDate} onChange={e=>setIssueDate(e.target.value)} required/></label><label>Válido hasta<input type="date" value={validUntil} onChange={e=>setValidUntil(e.target.value)}/></label><label>Forma de pago<select value={paymentMethodId??''} onChange={e=>setPaymentMethodId(e.target.value?Number(e.target.value):null)}><option value="">Sin especificar</option>{(opts?.paymentMethods??[]).map((x:any)=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>Condiciones de pago<select value={paymentTermId??''} onChange={e=>setPaymentTermId(e.target.value?Number(e.target.value):null)}><option value="">Sin especificar</option>{(opts?.paymentTerms??[]).map((x:any)=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label>IVA<select value={taxRateId??''} onChange={e=>{const id=e.target.value?Number(e.target.value):null;const t=(opts?.taxRates??[]).find((x:any)=>x.id===id);setTaxRateId(id);setTaxPercent(Number(t?.rate??0));}}><option value="">Sin IVA</option>{(opts?.taxRates??[]).map((t:any)=><option key={t.id} value={t.id}>{t.rate}% · {t.label}</option>)}</select></label><AddressEditor title="Dirección de facturación" addresses={addresses} value={billingAddress} onChange={setBillingAddress} onSourceChange={setBillingId}/><AddressEditor title="Dirección de instalación" addresses={addresses} value={installationAddress} onChange={setInstallationAddress} onSourceChange={setInstallationId}/></div>}</section>
    <section className="panel quotation-lines-panel"><div className="panel-head"><div><h2>Líneas del presupuesto</h2><p>Cada línea representa la configuración y el snapshot inmutable del artículo.</p></div><div style={{display:'flex',gap:'8px'}}><button type="button" className="secondary-button" onClick={()=>setLines(xs=>[...xs,blank()])}><Plus size={15}/>Línea Rápida</button><button type="button" className="primary-button" onClick={()=>{const newIndex=lines.length;setLines(xs=>[...xs,blank()]);openLineConfigurator(newIndex);}}><Layers size={15}/>Configurar Nueva Línea</button></div></div>
      <div className="table-panel quotation-lines-table"><table><thead><tr><th className="col-line-no">#</th><th className="col-article">Artículo</th><th className="col-description">Descripción & Configuración</th><th className="col-quantity">Cantidad</th><th className="col-price">Precio</th><th className="col-discount">Dto. %</th><th className="col-total">Total</th><th className="col-actions">Acciones</th></tr></thead><tbody>{lines.map((line,i)=>{const total=Math.max(0,line.quantity*line.unit_price*(1-line.discount_percent/100))*(1+taxPercent/100);const snapshot=line.configuration_snapshot||(line.specific_data?.configuration_snapshot as QuotationLineSnapshot|undefined);return <QuotationLineRows key={`line-${i}`} line={line} lineIndex={i} total={total} snapshot={snapshot??null} expanded={expandedLines.has(i)} opts={opts} onProductChange={id=>void selectProduct(i,id)} onToggle={()=>toggleLine(i)} onOpenConfigurator={()=>openLineConfigurator(i)} onOpenSnapshot={()=>snapshot&&openSnapshotModal(snapshot,i+1)} onLinePatch={patch=>{const nextSpecific={...line.specific_data,...(patch.unit_price!==undefined?{price_missing:false,price_missing_reason:null}:{})};updateLine(i,{...patch,specific_data:nextSpecific});}} onRemove={()=>setLines(xs=>xs.filter((_,j)=>j!==i))} canRemove={lines.length>1} onDimensionChange={updateDimensionValue} onCharacteristicChange={updateCharacteristic}/>;})}</tbody></table></div>
      <div className="quote-totals"><span>Base imponible <strong>{money(totals.net)}</strong></span><span>Descuentos <strong>{money(totals.discount)}</strong></span><span>Impuestos ({taxPercent}%) <strong>{money(totals.tax)}</strong></span><span>Total <strong>{money(totals.total)}</strong></span></div></section>
    <section className="panel quotation-notes-panel"><label className="wide-field"><span className="sr-only">Observaciones</span><textarea aria-label="Observaciones" placeholder="Observaciones generales del presupuesto…" rows={4} value={notes} onChange={e=>setNotes(e.target.value)}/></label></section>
  </form><ProfileSaveBar onSave={()=>{const formElement=document.getElementById('quotation-create-form') as HTMLFormElement|null;formElement?.requestSubmit();}} saving={saving} label="Crear presupuesto"/>
  {pendingCustomerId!==null&&<ConfirmationDialog title="Cambiar cliente" message="Al cambiar el cliente se recalcularán las direcciones y los descuentos aplicados a las líneas del presupuesto. ¿Quieres continuar?" confirmLabel="Cambiar cliente" busy={changingCustomer} onCancel={cancelCustomerChange} onConfirm={confirmCustomerChange}/>} {configuratorOpen&&<QuotationLineConfigurator isOpen={configuratorOpen} onClose={()=>setConfiguratorOpen(false)} onConfirm={handleConfiguratorConfirm} initialProductId={currentConfiguratorLine?.product_id??null} initialSnapshot={currentConfiguratorLine?.configuration_snapshot??(currentConfiguratorLine?.specific_data?.configuration_snapshot as QuotationLineSnapshot|null)??null} initialQuantity={currentConfiguratorLine?.quantity??1} initialDiscount={currentConfiguratorLine?.discount_percent??0} taxPercent={taxPercent} companyId={1} warehouseId={warehouseId} productsList={opts?.products??[]}/>} {snapshotModalOpen&&selectedSnapshot&&<QuotationLineSnapshotModal isOpen={snapshotModalOpen} onClose={()=>setSnapshotModalOpen(false)} snapshot={selectedSnapshot} lineNo={selectedSnapshotLineNo}/>}</div>;
}

function QuotationLineRows({line,lineIndex,total,snapshot,expanded,opts,onProductChange,onToggle,onOpenConfigurator,onOpenSnapshot,onLinePatch,onRemove,canRemove,onDimensionChange,onCharacteristicChange}:{line:Line;lineIndex:number;total:number;snapshot:QuotationLineSnapshot|null;expanded:boolean;opts:any;onProductChange:(id:number|null)=>void;onToggle:()=>void;onOpenConfigurator:()=>void;onOpenSnapshot:()=>void;onLinePatch:(patch:Partial<Line>)=>void;onRemove:()=>void;canRemove:boolean;onDimensionChange:(line:number,dimension:number,value:number|null)=>void;onCharacteristicChange:(line:number,characteristic:number,patch:Partial<QuotationLineCharacteristicDraft>)=>void}){
 const isConfigured=Boolean(snapshot);const priceMissing=Boolean(line.specific_data?.price_missing);const isConfigurable=Boolean(line.line_behavior?.configuration_enabled||line.line_behavior?.dimensions_enabled||line.line_behavior?.characteristics_enabled||line.line_behavior?.cut_calculation_enabled||line.line_behavior?.canvas_cut_enabled||(line.product_definition_snapshot?.dimensions&&line.product_definition_snapshot.dimensions.length>0)||(line.product_definition_snapshot?.characteristics&&line.product_definition_snapshot.characteristics.length>0));
 return <><tr className={`quotation-line-row ${isConfigured?'line-has-snapshot':''} ${priceMissing?'line-price-missing':''}`}><td className="col-line-no"><span className="line-num-badge">{lineIndex+1}</span></td><td className="col-article"><LookupSelect compact options={opts?.products??[]} value={line.product_id} onChange={onProductChange} placeholder="Buscar artículo…"/>{line.product_id&&<div className="line-article-badges">{priceMissing?<span className="line-price-warning" title={String(line.specific_data?.price_missing_reason||'No existe precio para esta combinación.')}>⚠ Precio pendiente: añádelo manualmente</span>:isConfigured?<button type="button" className="line-status-chip configured" onClick={onOpenConfigurator} title="Artículo configurado. Clic para editar"><Sparkles size={11}/>Configurado</button>:isConfigurable?<button type="button" className="line-status-chip pending" onClick={onOpenConfigurator} title="Artículo configurable. Clic para configurar"><SlidersHorizontal size={11}/>Configurar</button>:null}</div>}</td><td className="col-description"><input className="line-text-input" value={line.description} onChange={e=>onLinePatch({description:e.target.value})} placeholder="Descripción del artículo o partida…" required/>{snapshot&&<div className="line-config-summary">{snapshot.dimensions&&snapshot.dimensions.length>0&&<span className="summary-pill dim">📐 {snapshot.dimensions.map(d=>`${d.name}: ${d.value??0} ${d.unit_code}`).join(' · ')}</span>}{snapshot.selected_variant&&<span className="summary-pill variant">🏷️ {snapshot.selected_variant.code||snapshot.selected_variant.description}</span>}{snapshot.breakdown?.components&&snapshot.breakdown.components.length>0&&<span className="summary-pill bom">📦 {snapshot.breakdown.components.length} comp.</span>}</div>}</td><td className="col-quantity"><input className="line-num-input" type="number" min="0.01" step="0.01" value={line.quantity} onChange={e=>onLinePatch({quantity:Number(e.target.value)})}/></td><td className="col-price"><input className={`line-num-input ${priceMissing?'price-input-missing':''}`} type="number" min="0" step="0.01" value={line.unit_price} onChange={e=>onLinePatch({unit_price:Number(e.target.value)})}/>{priceMissing&&<small className="price-missing-help">No hay precio para esta combinación. Introduce el precio manualmente.</small>}</td><td className="col-discount"><input className="line-num-input" type="number" min="0" max="100" step="0.01" value={line.discount_percent} onChange={e=>onLinePatch({discount_percent:Number(e.target.value)})}/></td><td className="col-total"><strong className="line-total-val">{money(total)}</strong></td><td className="col-actions"><div className="line-actions-wrap">{line.product_id&&<button type="button" className={`line-btn-icon ${isConfigured?'active':''}`} onClick={onOpenConfigurator} title="Configurador guiado" aria-label="Configurador guiado"><SlidersHorizontal size={14}/></button>}{snapshot&&<button type="button" className="line-btn-icon snapshot" onClick={onOpenSnapshot} title="Ver snapshot y despiece congelado" aria-label="Ver snapshot y despiece congelado"><Eye size={14}/></button>}{line.product_id&&line.product_definition_snapshot&&<button type="button" className={`line-btn-icon toggle ${expanded?'expanded':''}`} onClick={onToggle} title={expanded?'Ocultar valores rápidos':'Desplegar valores rápidos'} aria-expanded={expanded}>{expanded?<ChevronUp size={14}/>:<ChevronDown size={14}/>}</button>}<button type="button" className="line-btn-icon delete" disabled={!canRemove} aria-label="Eliminar línea" title="Eliminar línea" onClick={onRemove}><Trash2 size={14}/></button></div></td></tr>{expanded&&line.product_id&&line.product_definition_snapshot&&<tr className="line-details-row"><td colSpan={8}><QuotationLineInstanceEditor line={line} lineIndex={lineIndex} onDimensionChange={onDimensionChange} onCharacteristicChange={onCharacteristicChange} onLinePatch={onLinePatch}/></td></tr>}</>;
}

function QuotationLineInstanceEditor({line,lineIndex,onDimensionChange,onCharacteristicChange,onLinePatch}:{line:Line;lineIndex:number;onDimensionChange:(line:number,dimension:number,value:number|null)=>void;onCharacteristicChange:(line:number,characteristic:number,patch:Partial<QuotationLineCharacteristicDraft>)=>void;onLinePatch:(patch:Partial<Line>)=>void}){
 const definition=line.product_definition_snapshot;if(!definition)return null;
 return <div className="quotation-line-editor-card"><div className="quotation-definition-summary"><div><strong>Configuración del artículo</strong><span>{definition.dimensions.length} dimensión(es) · {definition.characteristics.length} característica(s)</span></div><span className="definition-badge">Definición bloqueada</span></div><div className="quotation-instance-grid">{definition.dimensions.length>0&&<div className="quotation-edit-subsection"><div className="quotation-section-head"><div><h4>Dimensiones</h4><p>El nombre y la unidad vienen definidos por el artículo.</p></div></div>{line.dimensions.map((d,di)=><div className="quotation-instance-row" key={`${d.code}-${di}`}><div><strong>{d.name}</strong><span>{d.unit_id!=null?`Unidad ${d.unit_id}`:''}</span></div><input aria-label={`Valor ${d.name}`} type="number" step={1/10**Math.max(0,Number(definition.dimensions[di]?.decimals??2))} value={d.value??''} onChange={e=>onDimensionChange(lineIndex,di,e.target.value===''?null:Number(e.target.value))}/></div>)}</div>}{definition.characteristics.length>0&&<div className="quotation-edit-subsection"><div className="quotation-section-head"><div><h4>Características</h4><p>Solo aparecen las características asociadas al artículo.</p></div></div>{line.characteristics.map((current,ci)=>{const c=definition.characteristics[ci];if(!c)return null;return <div className="quotation-instance-row" key={c.assignment_id}><div><strong>{c.attribute_name||c.attribute_code}</strong><span>{c.required?'Obligatorio':''}</span></div>{c.values.length>0?<select value={current.attribute_value_id??''} onChange={e=>onCharacteristicChange(lineIndex,ci,{attribute_id:c.attribute_id,attribute_value_id:e.target.value?Number(e.target.value):null,value_text:null,value_number:null,value_boolean:null})}><option value="">Selecciona…</option>{c.values.map(v=><option key={v.id} value={v.id}>{v.name}</option>)}</select>:c.data_type==='NUMBER'?<input type="number" step="0.01" value={current.value_number??''} onChange={e=>onCharacteristicChange(lineIndex,ci,{attribute_id:c.attribute_id,attribute_value_id:null,value_text:null,value_number:e.target.value===''?null:Number(e.target.value),value_boolean:null})}/>:c.data_type==='BOOLEAN'?<input type="checkbox" checked={current.value_boolean===true} onChange={e=>onCharacteristicChange(lineIndex,ci,{attribute_id:c.attribute_id,attribute_value_id:null,value_text:null,value_number:null,value_boolean:e.target.checked})}/>:<input value={current.value_text??''} onChange={e=>onCharacteristicChange(lineIndex,ci,{attribute_id:c.attribute_id,attribute_value_id:null,value_text:e.target.value||null,value_number:null,value_boolean:null})}/>}</div>})}</div>}{definition.dimensions.length===0&&definition.characteristics.length===0&&<p className="muted">Este artículo no tiene configuración.</p>}</div></div>;
}

function AddressEditor({title,addresses,value,onChange,onSourceChange}:{title:string;addresses:any[];value:AddressDraft;onChange:(value:AddressDraft)=>void;onSourceChange:(id:number|null)=>void}){const set=(key:keyof AddressDraft,next:string)=>onChange({...value,[key]:next});const selectSource=(id:number|null)=>{const source=addresses.find(a=>a.id===id);onSourceChange(id);onChange(source?addressDraft(source):{...value,source_id:id});};return <fieldset className="address-editor"><legend>{title}</legend><label className="address-source">Cargar desde cliente<select value={value.source_id??''} onChange={e=>selectSource(e.target.value?Number(e.target.value):null)}><option value="">Sin dirección de cliente</option>{addresses.map(a=><option key={a.id} value={a.id}>{addressLabel(a)}</option>)}</select></label><div className="address-editor-grid"><label className="address-street">Dirección<input value={value.street} onChange={e=>set('street',e.target.value)}/></label><label>Código Postal<input value={value.postal_code} onChange={e=>set('postal_code',e.target.value)}/></label><label>Localidad<input value={value.city} onChange={e=>set('city',e.target.value)}/></label><label>Provincia<input value={value.region} onChange={e=>set('region',e.target.value)}/></label></div></fieldset>;}
function addressDraft(a:any):AddressDraft{return{source_id:a?.id??null,label:a?.label||({FACTURACION:'Facturación',FISCAL:'Fiscal',INSTALACION:'Instalación'} as Record<string,string>)[String(a?.address_type||'').toUpperCase()]||'Dirección',street:a?.street||'',postal_code:a?.postal_code||'',city:a?.city||'',region:a?.region||''};}
function addressLabel(a:any){return[a.label||({FACTURACION:'Facturación',FISCAL:'Fiscal',INSTALACION:'Instalación'} as Record<string,string>)[String(a.address_type).toUpperCase()]||'Dirección',a.street,a.postal_code,a.city].filter(Boolean).join(' · ');}

function LookupSelect({label,required=false,compact=false,options=[],value,onChange,placeholder}:{label?:string;required?:boolean;compact?:boolean;options?:Option[];value:number|null;onChange:(id:number|null)=>void;placeholder:string}){const [query,setQuery]=useState('');const [open,setOpen]=useState(false);const [rect,setRect]=useState<DOMRect|null>(null);const inputRef=useRef<HTMLInputElement|null>(null);const safeOptions=options||[];const selected=safeOptions.find(x=>x.id===value);const filtered=useMemo(()=>{const q=query.trim().toLocaleLowerCase();if(!q)return safeOptions.slice(0,12);return safeOptions.filter(x=>`${x.code??''} ${x.label}`.toLocaleLowerCase().includes(q)).slice(0,12);},[safeOptions,query]);const reposition=()=>{if(inputRef.current)setRect(inputRef.current.closest('.lookup-control')?.getBoundingClientRect()??inputRef.current.getBoundingClientRect());};useEffect(()=>{if(!open)return;reposition();const onScroll=()=>reposition();window.addEventListener('scroll',onScroll,true);window.addEventListener('resize',onScroll);return()=>{window.removeEventListener('scroll',onScroll,true);window.removeEventListener('resize',onScroll);};},[open]);const selectItem=(id:number)=>{onChange(id);setQuery('');setOpen(false);};return <div className={`lookup-field ${compact?'lookup-field-compact':''}`}>{label&&<span className="field-label">{label}{required?' *':''}</span>}<div className="lookup-control"><Search size={15}/><input ref={inputRef} required={required&&!value} value={open?query:selected?.label??''} placeholder={placeholder} onFocus={()=>{setOpen(true);if(selected)setQuery(selected.label);}} onChange={e=>{const next=e.target.value;setQuery(next);setOpen(true);if(value!==null)onChange(null);const exact=safeOptions.find(x=>x.code&&x.code.trim().toLowerCase()===next.trim().toLowerCase());if(exact)selectItem(exact.id);}} onKeyDown={e=>{if(e.key==='Enter'){e.preventDefault();if(filtered.length>0)selectItem(filtered[0].id);}else if(e.key==='Escape')setOpen(false);}}/>{selected&&<button type="button" className="lookup-clear" aria-label="Limpiar selección" onClick={()=>{setQuery('');onChange(null);setOpen(false);}}><X size={14}/></button>}</div>{open&&rect&&createPortal(<div className="lookup-portal"><button type="button" className="lookup-dismiss" aria-label="Cerrar resultados" onClick={()=>setOpen(false)}/><div className="lookup-results" style={{top:rect.bottom+4,left:rect.left,width:Math.max(rect.width,280)}}>{filtered.length===0?<small>No se han encontrado resultados.</small>:filtered.map(x=><button type="button" key={x.id} onMouseDown={e=>e.preventDefault()} onClick={()=>selectItem(x.id)} style={{display:'flex',justifyContent:'space-between',alignItems:'center',width:'100%'}}><span><strong>{x.code?`${x.code} · `:''}</strong>{x.label}</span>{x.price!=null&&<span style={{fontSize:'11px',color:'#64748b',marginLeft:'8px'}}>{money(Number(x.price))}</span>}</button>)}</div></div>,document.body)}</div>;}
function ConfirmationDialog({title,message,confirmLabel,busy,onCancel,onConfirm}:{title:string;message:string;confirmLabel:string;busy:boolean;onCancel:()=>void;onConfirm:()=>void}){return createPortal(<div className="confirmation-overlay" role="presentation"><div className="confirmation-dialog" role="dialog" aria-modal="true" aria-labelledby="confirmation-title"><h2 id="confirmation-title">{title}</h2><p id="confirmation-message">{message}</p><div className="confirmation-actions"><button type="button" className="secondary-button" onClick={onCancel} disabled={busy}>Cancelar</button><button type="button" className="primary-button" onClick={onConfirm} disabled={busy}>{busy?'Recalculando…':confirmLabel}</button></div></div></div>,document.body);}
