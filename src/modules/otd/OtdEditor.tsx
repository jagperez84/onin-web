import { FormEvent,useEffect,useMemo,useState } from 'react';
import { ArrowLeft,Plus,Save,Search,Trash2,WandSparkles,X } from 'lucide-react';
import { NavLink,useNavigate,useParams } from 'react-router-dom';
import { supabase } from '../../lib/supabase';
import './otd.css';

type Otd={id:number;company_id:number;code:string;name:string;template_type:string|null;active:boolean};type Selection={id:number;code:string;name:string;selection_type:string;required:boolean;is_dimension:boolean;sort_order:number;options?:Option[]};type Option={id:number;code:string;label:string;value:string|null;sort_order:number};type Variable={id:number;code:string;name:string;expression:string|null;data_type:string;min_value:number|null;max_value:number|null;sort_order:number;active:boolean};type Dimension={id:number;measurement_type_id:number;dimension_number:number;code:string;name:string;unit_id:number|null;decimals:number;unit?:{id:number;code:string;name:string}|null};type MeasurementType={id:number;code:string;name:string;dimension_count:number;dimensions:Dimension[]};type Characteristic={id:number;product_id:number;code:string;description:string|null;pvp:number|null;price_increment:number;active:boolean};type Product={id:number;code:string;family_id:number|null;technical_description:string|null;commercial_description:string|null;measurement_type_id:number|null;measurement_type?:MeasurementType|null;characteristics:Characteristic[]};type Component={id:number;product_id:number|null;characteristic_id:number|null;characteristic_expression:string|null;code:string;description:string|null;quantity_expression:string|null;dimension_expressions:Record<string,string>;component_type:'BASIC'|'IMPROVEMENT';price_increment:number;price_increment_type:'FIXED'|'PERCENTAGE';active:boolean;sort_order:number};
const emptySelection=():Selection=>({id:0,code:'',name:'',selection_type:'OPTION',required:false,is_dimension:false,sort_order:0,options:[]});const emptyVariable=():Variable=>({id:0,code:'',name:'',expression:'',data_type:'NUMBER',min_value:null,max_value:null,sort_order:0,active:true});const emptyComponent=():Component=>({id:0,product_id:null,characteristic_id:null,characteristic_expression:null,code:'',description:'',quantity_expression:'1',dimension_expressions:{},component_type:'BASIC',price_increment:0,price_increment_type:'FIXED',active:true,sort_order:0});
export function OtdEditor(){const{id}=useParams();const navigate=useNavigate();const editing=Boolean(id);const[otd,setOtd]=useState<Otd>({id:0,company_id:0,code:'',name:'',template_type:'TOLDO',active:true});const[selections,setSelections]=useState<Selection[]>([]);const[variables,setVariables]=useState<Variable[]>([]);const[components,setComponents]=useState<Component[]>([]);const[products,setProducts]=useState<Record<number,Product>>({});const[productResults,setProductResults]=useState<Product[]>([]);const[activeProductComponent,setActiveProductComponent]=useState<number|null>(null);const[productSearch,setProductSearch]=useState('');const[naturalRule,setNaturalRule]=useState('');const[loading,setLoading]=useState(editing);const[saving,setSaving]=useState(false);const[message,setMessage]=useState('');
 useEffect(()=>{if(!editing||!supabase){setLoading(false);return}let cancelled=false;(async()=>{const oid=Number(id);const[o,s,v,c]=await Promise.all([supabase.from('otd').select('*').eq('id',oid).single(),supabase.from('otd_selection').select('*,otd_selection_option(*)').eq('otd_id',oid).order('sort_order'),supabase.from('otd_variable').select('*').eq('otd_id',oid).order('sort_order'),supabase.from('otd_component').select('*').eq('otd_id',oid).order('sort_order')]);if(cancelled)return;if(o.data)setOtd(o.data);if(s.data)setSelections(s.data.map((x:any)=>({...x,is_dimension:Boolean(x.is_dimension),options:x.otd_selection_option??[]})));if(v.data)setVariables(v.data);if(c.data){const loaded=(c.data as any[]).map(x=>({...x,product_id:x.product_id??null,characteristic_id:x.characteristic_id??null,characteristic_expression:x.characteristic_expression??null,component_type:x.component_type==='IMPROVEMENT'?'IMPROVEMENT':'BASIC',price_increment:Number(x.price_increment??0),price_increment_type:x.price_increment_type==='PERCENTAGE'?'PERCENTAGE':'FIXED',quantity_expression:x.quantity_expression||'1',dimension_expressions:x.dimension_expressions&&typeof x.dimension_expressions==='object'?x.dimension_expressions:{}}))as Component[];setComponents(loaded);await loadProducts(loaded.map(x=>x.product_id).filter((x):x is number=>Number.isFinite(x)))}setLoading(false)})();return()=>{cancelled=true}},[id,editing]);
 async function hydrate(rows:any[]):Promise<Product[]>{if(!supabase||!rows.length)return[];const familyIds=[...new Set(rows.map(p=>p.family_id).filter((x:any):x is number=>Number.isFinite(x)))];const families=familyIds.length?(await supabase.from('product_family').select('id,measurement_type_id').in('id',familyIds)).data??[]:[];const familyMap=Object.fromEntries(families.map((f:any)=>[f.id,f]));const mtIds=[...new Set(rows.map(p=>Number.isFinite(p.measurement_type_id)?p.measurement_type_id:familyMap[p.family_id]?.measurement_type_id).filter((x:any):x is number=>Number.isFinite(x)))];const mts=mtIds.length?(await supabase.from('measurement_type').select('id,code,name,dimension_count').in('id',mtIds)).data??[]:[];const dims=mtIds.length?(await supabase.from('measurement_type_dimension').select('id,measurement_type_id,dimension_number,code,name,unit_id,decimals').in('measurement_type_id',mtIds).order('dimension_number')).data??[]:[];const unitIds=[...new Set(dims.map((d:any)=>d.unit_id).filter((x:any):x is number=>Number.isFinite(x)))];const units=unitIds.length?(await supabase.from('unit').select('id,code,name').in('id',unitIds)).data??[]:[];const um=Object.fromEntries(units.map((u:any)=>[u.id,u]));const mtMap=Object.fromEntries(mts.map((m:any)=>[m.id,{...m,dimensions:dims.filter((d:any)=>d.measurement_type_id===m.id).map((d:any)=>({...d,unit:um[d.unit_id]??null}))}]));const ids=rows.map(p=>Number(p.id)).filter(Number.isFinite);const ch=ids.length?(await supabase.from('product_characteristic').select('id,product_id,code,description,pvp,price_increment,active').in('product_id',ids).eq('active',true).is('deleted_at',null).order('code')).data??[]:[];const cm=new Map<number,Characteristic[]>();for(const x of ch as Characteristic[]){const l=cm.get(Number(x.product_id))??[];l.push(x);cm.set(Number(x.product_id),l)}return rows.map(p=>{const mtid=Number.isFinite(p.measurement_type_id)?p.measurement_type_id:familyMap[p.family_id]?.measurement_type_id??null;return{...p,measurement_type_id:mtid,measurement_type:mtid!=null?mtMap[mtid]??null:null,characteristics:cm.get(Number(p.id))??[]}})as Product[]}
 async function loadProducts(ids:number[]){if(!supabase||!ids.length)return;const{data,error}=await supabase.from('product').select('id,code,family_id,technical_description,commercial_description,measurement_type_id').in('id',[...new Set(ids)]);if(error||!data)return;const hydrated=await hydrate(data);setProducts(prev=>({...prev,...Object.fromEntries(hydrated.map(p=>[p.id,p]))}))}
 async function searchProducts(term:string){setProductSearch(term);if(!supabase||term.trim().length<2){setProductResults([]);return}const q=term.trim();const{data,error}=await supabase.from('product').select('id,code,family_id,technical_description,commercial_description,measurement_type_id').eq('active',true).or(`code.ilike.%${q}%,technical_description.ilike.%${q}%,commercial_description.ilike.%${q}%`).order('code').limit(8);if(error||!data){setProductResults([]);return}setProductResults(await hydrate(data))}
 function selectProduct(index:number,p:Product){const next=[...components];next[index]={...next[index],product_id:p.id,characteristic_id:null,characteristic_expression:null,code:p.code,description:p.commercial_description||p.technical_description||p.code,dimension_expressions:Object.fromEntries((p.measurement_type?.dimensions??[]).map(d=>[d.code,'']))};setComponents(next);setProducts(prev=>({...prev,[p.id]:p}));setActiveProductComponent(null);setProductSearch('');setProductResults([])}function clearProduct(index:number){const next=[...components];next[index]={...next[index],product_id:null,characteristic_id:null,characteristic_expression:null,code:'',description:'',dimension_expressions:{}};setComponents(next)}function updateComponent(index:number,patch:Partial<Component>){const next=[...components];next[index]={...next[index],...patch};setComponents(next)}
 async function companyId(){if(!supabase)throw new Error('Supabase no está configurado');const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Sesión no disponible');const{data,error}=await supabase.from('user_account').select('company_id').eq('auth_user_id',user.id).single();if(error||!data)throw new Error('No se ha podido determinar la empresa del usuario');return Number(data.company_id)}
 async function save(e?:FormEvent){e?.preventDefault();if(!supabase)return setMessage('Supabase no está configurado.');if(!otd.code.trim()||!otd.name.trim())return setMessage('Código y nombre son obligatorios.');const invalid=components.findIndex(c=>!c.product_id);if(invalid>=0)return setMessage(`El componente ${invalid+1} debe tener un artículo de ONIN seleccionado.`);setSaving(true);setMessage('');try{const company_id=otd.company_id||await companyId();let oid=otd.id;if(!oid){const{data,error}=await supabase.from('otd').insert({company_id,code:otd.code.trim(),name:otd.name.trim(),template_type:otd.template_type||null,active:otd.active}).select().single();if(error)throw error;oid=data.id}else{const{error}=await supabase.from('otd').update({code:otd.code.trim(),name:otd.name.trim(),template_type:otd.template_type||null,active:otd.active,updated_at:new Date().toISOString()}).eq('id',oid);if(error)throw error}await supabase.from('otd_selection').delete().eq('otd_id',oid);await supabase.from('otd_variable').delete().eq('otd_id',oid);await supabase.from('otd_component').delete().eq('otd_id',oid);for(const[si,s]of selections.entries()){const{data:sd,error:se}=await supabase.from('otd_selection').insert({otd_id:oid,code:s.code,name:s.name,selection_type:s.selection_type,required:s.required,is_dimension:s.is_dimension,sort_order:si}).select().single();if(se)throw se;const opts=(s.options??[]).map((o,i)=>({selection_id:sd.id,code:o.code,label:o.label,value:o.value??null,sort_order:i}));if(opts.length){const{error}=await supabase.from('otd_selection_option').insert(opts);if(error)throw error}}}const vars=variables.map((v,i)=>({otd_id:oid,code:v.code,name:v.name,expression:v.expression||null,data_type:v.data_type,min_value:v.min_value,max_value:v.max_value,sort_order:i,active:v.active}));if(vars.length){const{error}=await supabase.from('otd_variable').insert(vars);if(error)throw error}const comps=components.map((c,i)=>({otd_id:oid,product_id:c.product_id,characteristic_id:c.characteristic_id||null,characteristic_expression:c.characteristic_expression?.trim()||null,code:c.code,description:c.description||null,quantity_expression:c.quantity_expression||'1',component_type:c.component_type,price_increment:c.component_type==='IMPROVEMENT'?Number(c.price_increment||0):0,price_increment_type:c.component_type==='IMPROVEMENT'?c.price_increment_type:'FIXED',active:c.active,sort_order:i,dimension_expressions:c.dimension_expressions||{}}));if(comps.length){const{error}=await supabase.from('otd_component').insert(comps);if(error)throw error}const{data:allS}=await supabase.from('otd_selection').select('*,otd_selection_option(*)').eq('otd_id',oid).order('sort_order');const{data:allV}=await supabase.from('otd_variable').select('*').eq('otd_id',oid).order('sort_order');const{data:allC}=await supabase.from('otd_component').select('*').eq('otd_id',oid).order('sort_order');const nextVersion=((await supabase.from('otd_version').select('version_number').eq('otd_id',oid).order('version_number',{ascending:false}).limit(1).maybeSingle()).data?.version_number??0)+1;const snapshot={otd:{...otd,id:oid,company_id},selections:allS??[],variables:allV??[],components:allC??[],natural_rule:naturalRule};const{error:ve}=await supabase.from('otd_version').insert({otd_id:oid,version_number:nextVersion,snapshot});if(ve)throw ve;setOtd(x=>({...x,id:oid,company_id}));setMessage(`Guardado correctamente. Versión ${nextVersion}.`);if(!editing)navigate(`/produccion/otd/${oid}`,{replace:true})}catch(err:any){setMessage(err?.message??'No se ha podido guardar.')}finally{setSaving(false)}}
 if(loading)return <div className="otd-page"><div className="otd-empty">Cargando OTD…</div></div>;return <div className="otd-page"><div className="otd-head"><div><NavLink to="/produccion/otd" className="otd-back"><ArrowLeft size={15}/> OTD</NavLink><div className="eyebrow">EDITOR TÉCNICO</div><h1>{editing?otd.name||'Editar OTD':'Nuevo OTD'}</h1><p>Construye el producto a partir de artículos reales de ONIN.</p></div><button className="primary-btn" onClick={()=>save()} disabled={saving}><Save size={16}/>{saving?'Guardando…':'Guardar OTD'}</button></div><form onSubmit={save}>
<section className="otd-card"><div className="otd-card-head"><div><h2>Identificación</h2><p>Definición del artículo compuesto.</p></div></div><div className="otd-grid three"><label>Código<input value={otd.code} onChange={e=>setOtd({...otd,code:e.target.value})}/></label><label>Nombre<input value={otd.name} onChange={e=>setOtd({...otd,name:e.target.value})}/></label><label>Tipo<select value={otd.template_type??''} onChange={e=>setOtd({...otd,template_type:e.target.value})}><option value="TOLDO">Toldo</option><option value="">Genérico</option></select></label></div></section>
<section className="otd-card"><div className="otd-card-head"><div><h2>Entradas para oficina</h2><p>Lo que el usuario seleccionará al añadir el artículo al presupuesto.</p></div><button type="button" className="secondary-btn" onClick={()=>setSelections(x=>[...x,emptySelection()])}><Plus size={15}/> Añadir entrada</button></div>{selections.length===0?<div className="otd-empty">Todavía no hay entradas.</div>:selections.map((s,si)=><div className="otd-row-card" key={si}><div className="otd-row-actions"><strong>{si+1}. Entrada</strong><button type="button" className="icon-btn danger" onClick={()=>setSelections(x=>x.filter((_,i)=>i!==si))}><Trash2 size={15}/></button></div><div className="otd-grid four"><label>Código<input value={s.code} onChange={e=>{const x=[...selections];x[si]={...s,code:e.target.value};setSelections(x)}}/></label><label>Nombre<input value={s.name} onChange={e=>{const x=[...selections];x[si]={...s,name:e.target.value};setSelections(x)}}/></label><label>Tipo<select value={s.selection_type} onChange={e=>{const x=[...selections];x[si]={...s,selection_type:e.target.value};setSelections(x)}}><option>OPTION</option><option>NUMBER</option><option>TEXT</option></select></label><label className="check"><input type="checkbox" checked={s.required} onChange={e=>{const x=[...selections];x[si]={...s,required:e.target.checked};setSelections(x)}}/> Obligatorio</label><label className="check"><input type="checkbox" checked={s.is_dimension} onChange={e=>{const x=[...selections];x[si]={...s,is_dimension:e.target.checked};setSelections(x)}}/> Es dimensión</label></div>{s.selection_type==='OPTION'&&<div className="option-list">{(s.options??[]).map((o,oi)=><div className="option-line" key={oi}><input placeholder="Código" value={o.code} onChange={e=>{const x=[...selections];const opts=[...(s.options??[])];opts[oi]={...o,code:e.target.value};x[si]={...s,options:opts};setSelections(x)}}/><input placeholder="Etiqueta" value={o.label} onChange={e=>{const x=[...selections];const opts=[...(s.options??[])];opts[oi]={...o,label:e.target.value};x[si]={...s,options:opts};setSelections(x)}}/><input placeholder="Valor" value={o.value??''} onChange={e=>{const x=[...selections];const opts=[...(s.options??[])];opts[oi]={...o,value:e.target.value};x[si]={...s,options:opts};setSelections(x)}}/><button type="button" className="icon-btn danger" onClick={()=>{const x=[...selections];x[si]={...s,options:(s.options??[]).filter((_,i)=>i!==oi)};setSelections(x)}}><Trash2 size={14}/></button></div>)}<button type="button" className="link-btn" onClick={()=>{const x=[...selections];x[si]={...s,options:[...(s.options??[]),{id:0,code:'',label:'',value:'',sort_order:(s.options??[]).length}]};setSelections(x)}}><Plus size={13}/> Añadir opción</button></div>}</div>)}</section>
<section className="otd-card"><div className="otd-card-head"><div><h2>Formulación</h2><p>El técnico define la lógica; oficina no necesita conocerla.</p></div><span className="ai-badge"><WandSparkles size={14}/> IA preparada</span></div><label>Regla en lenguaje natural<textarea value={naturalRule} onChange={e=>setNaturalRule(e.target.value)} placeholder="Ejemplo: si el ancho supera 4 metros, añadir un soporte central; la cantidad de lona es ancho por salida."/></label><div className="hint">Variables disponibles: {variables.filter(v=>v.code).map(v=>v.code).join(', ')||'todavía no definidas'}.</div>{variables.map((v,vi)=><div className="otd-rule-line" key={vi}><input placeholder="Código" value={v.code} onChange={e=>{const x=[...variables];x[vi]={...v,code:e.target.value};setVariables(x)}}/><input placeholder="Nombre" value={v.name} onChange={e=>{const x=[...variables];x[vi]={...v,name:e.target.value};setVariables(x)}}/><input className="wide" placeholder="Expresión técnica (opcional)" value={v.expression??''} onChange={e=>{const x=[...variables];x[vi]={...v,expression:e.target.value};setVariables(x)}}/><button type="button" className="icon-btn danger" onClick={()=>setVariables(x=>x.filter((_,i)=>i!==vi))}><Trash2 size={14}/></button></div>)}<button type="button" className="secondary-btn" onClick={()=>setVariables(x=>[...x,emptyVariable()])}><Plus size={15}/> Añadir variable calculada</button></section>
<section className="otd-card"><div className="otd-card-head"><div><h2>Componentes del producto</h2><p>Cada componente es un artículo real de ONIN. Sus dimensiones se calculan y su precio se obtiene del escalado del artículo maestro.</p></div><button type="button" className="secondary-btn" onClick={()=>setComponents(x=>[...x,emptyComponent()])}><Plus size={15}/> Añadir artículo</button></div>{components.length===0?<div className="otd-empty">Todavía no hay artículos.</div>:components.map((c,ci)=>{const product=c.product_id?products[c.product_id]:undefined;const dimensions=product?.measurement_type?.dimensions??[];const characteristics=product?.characteristics??[];const dynamicCharacteristic=Boolean(c.characteristic_expression?.trim());return <div className="otd-row-card" key={c.id||ci}><div className="otd-row-actions"><strong>{ci+1}. Componente</strong><button type="button" className="icon-btn danger" onClick={()=>setComponents(x=>x.filter((_,i)=>i!==ci))}><Trash2 size={15}/></button></div><div className="otd-component-grid"><div className="otd-product-field"><span className="field-label">Artículo</span>{product?<div className="otd-product-selected"><div><strong>{product.code}</strong><span>{product.commercial_description||product.technical_description||'Sin descripción'}</span>{product.measurement_type&&<small>Tipo de medida: {product.measurement_type.name} · {product.measurement_type.dimension_count} dimensión(es)</small>}</div><button type="button" className="icon-btn" onClick={()=>{setActiveProductComponent(ci);setProductSearch(product.code);void searchProducts(product.code)}}><Search size={15}/></button><button type="button" className="icon-btn danger" onClick={()=>clearProduct(ci)}><X size={15}/></button></div>:<button type="button" className="product-select-empty" onClick={()=>{setActiveProductComponent(ci);setProductSearch('');setProductResults([])}}><Search size={16}/> Seleccionar artículo de ONIN</button>}{activeProductComponent===ci&&<div className="otd-product-picker"><div className="otd-product-search"><Search size={15}/><input autoFocus value={productSearch} onChange={e=>void searchProducts(e.target.value)} placeholder="Buscar por código o descripción…"/><button type="button" className="icon-btn" onClick={()=>{setActiveProductComponent(null);setProductResults([])}}><X size={14}/></button></div>{productResults.length>0?<div className="otd-product-results">{productResults.map(p=><button type="button" key={p.id} onClick={()=>selectProduct(ci,p)}><strong>{p.code}</strong><span>{p.commercial_description||p.technical_description||'Sin descripción'}</span>{p.characteristics.length>0&&<small>{p.characteristics.length} característica(s) · {p.measurement_type?`${p.measurement_type.name} · ${p.measurement_type.dimension_count} dimensión(es)`:''}</small>}</button>)}</div>:productSearch.length>=2?<div className="otd-product-no-results">No se han encontrado artículos.</div>:<div className="otd-product-no-results">Escribe al menos 2 caracteres.</div>}</div>}</div><label>Tipo<select value={c.component_type} onChange={e=>updateComponent(ci,{component_type:e.target.value as Component['component_type']})}><option value="BASIC">Básico</option><option value="IMPROVEMENT">Mejora</option></select></label><label>Cantidad / fórmula<input value={c.quantity_expression??''} onChange={e=>updateComponent(ci,{quantity_expression:e.target.value})} placeholder="Ej. 1"/></label>{c.component_type==='IMPROVEMENT'&&<><label>Tipo incremento<select value={c.price_increment_type} onChange={e=>updateComponent(ci,{price_increment_type:e.target.value as Component['price_increment_type']})}><option value="FIXED">Importe fijo (€)</option><option value="PERCENTAGE">Porcentaje (%)</option></select></label><label>Incremento<input type="number" min="0" step="0.01" value={c.price_increment} onChange={e=>updateComponent(ci,{price_increment:Number(e.target.value)||0})} placeholder="0"/></label></>}</div>{characteristics.length>0&&<div className="otd-characteristic-block"><div className="otd-dimensions-title"><strong>Característica / color</strong><span>Puede ser fija o resolverse desde una variable del sistema.</span></div><div className="otd-characteristic-grid"><label><span className="field-label">Origen</span><select value={dynamicCharacteristic?'VARIABLE':'FIXED'} onChange={e=>e.target.value==='VARIABLE'?updateComponent(ci,{characteristic_id:null}):updateComponent(ci,{characteristic_expression:null})}><option value="FIXED">Fija</option><option value="VARIABLE">Variable del sistema</option></select></label>{dynamicCharacteristic?<label><span className="field-label">Variable / expresión</span><input value={c.characteristic_expression??''} onChange={e=>updateComponent(ci,{characteristic_expression:e.target.value,characteristic_id:null})} placeholder="Ej. COLOR_CLIENTE"/></label>:<label><span className="field-label">Característica</span><select value={c.characteristic_id??''} onChange={e=>updateComponent(ci,{characteristic_id:e.target.value?Number(e.target.value):null,characteristic_expression:null})}><option value="">Seleccionar característica…</option>{characteristics.map(ch=><option key={ch.id} value={ch.id}>{ch.code}{ch.description?` · ${ch.description}`:''}</option>)}</select></label>}</div></div>}{dimensions.length>0&&<div className="otd-dimensions-block"><div className="otd-dimensions-title"><strong>Dimensiones del artículo</strong><span>El escalado utilizará estas dimensiones en el mismo orden que el Tipo de medida.</span></div><div className="otd-dimensions-grid">{dimensions.map(d=><label key={d.id}>{d.name}<span className="dimension-meta">{d.unit?.name||d.unit?.code||''}</span><input value={c.dimension_expressions?.[d.code]??''} onChange={e=>updateComponent(ci,{dimension_expressions:{...c.dimension_expressions,[d.code]:e.target.value}})} placeholder="Ej. ANCHO_UTIL"/></label>)}</div></div>}</div>})}</section>{message&&<div className={`otd-message ${message.startsWith('Guardado')?'ok':'error'}`}>{message}</div>}</form></div>}
export function OtdList(){const[rows,setRows]=useState<Otd[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{if(!supabase)return;void supabase.from('otd').select('*').order('name').then(({data})=>{setRows(data??[]);setLoading(false)})},[]);return <div className="otd-page"><div className="otd-head"><div><div className="eyebrow">PRODUCCIÓN</div><h1>OTD</h1><p>Configuradores técnicos de artículos compuestos.</p></div><NavLink to="/produccion/otd/nuevo" className="primary-btn"><Plus size={16}/> Nuevo OTD</NavLink></div><div className="otd-card">{loading?<div className="otd-empty">Cargando…</div>:rows.length===0?<div className="otd-empty">No hay OTD creados todavía.</div>:rows.map(r=><NavLink className="otd-list-row" key={r.id} to={`/produccion/otd/${r.id}`}><span><strong>{r.code}</strong><small>{r.name}</small></span><span className={r.active?'active-dot':'inactive-dot'}>{r.active?'Activo':'Inactivo'}</span></NavLink>)}</div></div>}
